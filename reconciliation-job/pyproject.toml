[project]
name = "reconciliation-job"
version = "0.1.0"
description = "PulseGrid reconciliation job — detects CRM/ERP mismatches and reports to LogPulse"
requires-python = ">=3.11"
dependencies = [
    "sqlalchemy>=2.0",
    "psycopg[binary]>=3.1",
    "httpx>=0.27",
    "pulsegrid-common",
]

[tool.uv.sources]
pulsegrid-common = { path = "../pulsegrid_common" }
